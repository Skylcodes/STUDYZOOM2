'use client';

// Import the TechStackVisual component instead of the original content
import { TechStackVisual } from '@/components/marketing/sections/tech-stack-visual';

export function RealResults(): React.JSX.Element {
    // Simply render the TechStackVisual component
    return <TechStackVisual />;
} 